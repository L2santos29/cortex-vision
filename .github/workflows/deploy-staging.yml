name: Deploy Staging

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build and Deploy to Staging
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: |
          docker build -t cortex-vision:staging -f Dockerfile .

      - name: Save image artifact
        run: |
          docker save cortex-vision:staging | gzip > cortex-vision-staging.tar.gz

      - uses: actions/upload-artifact@v4
        with:
          name: cortex-vision-staging
          path: cortex-vision-staging.tar.gz
          retention-days: 7

      # Opcional: desplegar en un servidor remoto vía SSH.
      # Para activarlo, configura los secrets en GitHub:
      #   STAGING_HOST, STAGING_USER, STAGING_SSH_KEY
      #- name: Deploy to staging server
      #  uses: appleboy/ssh-action@v1
      #  with:
      #    host: ${{ secrets.STAGING_HOST }}
      #    username: ${{ secrets.STAGING_USER }}
      #    key: ${{ secrets.STAGING_SSH_KEY }}
      #    script: |
      #      cd /opt/cortex-vision
      #      docker compose -f deploy/docker-compose.staging.yml pull
      #      docker compose -f deploy/docker-compose.staging.yml up -d --force-recreate
